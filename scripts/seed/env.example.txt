# Copy these into `.env.local` for `npm run seed:demo` (repo root).

# Already have URL + anon for the app — keep those.
# VITE_SUPABASE_URL=...
# VITE_SUPABASE_ANON_KEY=...

# Required for seed script only — from Dashboard → Project Settings → API
# Copy the long "service_role" secret (jwt). Do NOT add VITE_ prefix.
SUPABASE_SERVICE_ROLE_KEY=

# Any 8+ character string; demo users are created with this password.
NOMMI_SEED_AUTH_PASSWORD=
